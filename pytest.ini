[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
testpaths = dispenses oracle_accounts query tests
addopts = -vv --strict-markers
markers =
    integration_oracle: optional tests requiring a real Oracle environment
